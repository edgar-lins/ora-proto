version: '3.8'

services:
  db:
    image: postgres:16
    container_name: ora_db
    restart: always
    environment:
      POSTGRES_USER: ora
      POSTGRES_PASSWORD: ora123
      POSTGRES_DB: ora
    ports:
      - "5432:5432"
    volumes:
      - ./pgdata:/var/lib/postgresql/data
